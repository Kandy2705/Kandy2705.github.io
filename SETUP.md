# Portfolio setup

Public site: https://kandy2705.github.io

Admin: https://kandy2705.github.io/admin/

## Profile photo
Upload your portrait later as `/assets/profile.jpg`. The layout is already prepared for a portrait image.

## Supabase
1. Create a Supabase project.
2. Enable Google in Authentication > Providers.
3. Add `https://kandy2705.github.io/admin/` to allowed redirect URLs.
4. Put only the public Project URL and anon key into `/assets/config.js`.
5. Never put a service-role key in this repository.

Allowed admin email: `man.ngoman2705@gmail.com`.

The admin page falls back to browser-only demo storage until Supabase is configured.

Project categories: All, Web, Mobile App, Game, AR/VR, Research, Other.
Blog editor uses Markdown and publishes immediately; there is no draft workflow.